# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its mobile workflows focus without claiming live deployment or external usage.

## Cases

- `baseline`: `form pressure`, score 107, lane `watch`
- `stress`: `sync drift`, score 161, lane `ship`
- `edge`: `local state`, score 172, lane `ship`
- `recovery`: `conflict cost`, score 151, lane `ship`
- `stale`: `form pressure`, score 167, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
